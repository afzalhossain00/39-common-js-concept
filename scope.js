function add(a, b) {
    const total = a + b;
    console.log(a, b)
    if (b > 5) {
        const sum = 25 + a + b;
    }
    else {
        const sum = 20 + a + b
    }
    return total
}
add(20, 40)